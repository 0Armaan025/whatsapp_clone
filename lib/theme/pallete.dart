import 'package:hexcolor/hexcolor.dart';

class Pallete {
  // const Color textColor = HexColor()
  // I'd decide them later , hmm, after the app will be done
  // ui thingy will be done later, first imp thing is backend
  // 19:04:30 IST => 30-03-2024
}
